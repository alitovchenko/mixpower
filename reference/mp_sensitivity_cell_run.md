# Run one sensitivity grid cell (for serial and parallel workers)

Run one sensitivity grid cell (for serial and parallel workers)

## Usage

``` r
mp_sensitivity_cell_run(
  i,
  scenario,
  vary,
  grid,
  nsim,
  alpha,
  seed,
  failure_policy,
  conf_level,
  checkpoint_dir,
  resume
)
```
