% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{mp_plan}
\alias{mp_plan}
\title{Plan power the calibrate-first way}
\usage{
mp_plan(
  scenario,
  nsim = 1000,
  calibrate_nsim = nsim,
  alpha = 0.05,
  seed = NULL,
  conf_level = 0.95,
  ci_method = c("clopper-pearson", "wald"),
  failure_policy = c("count_as_nondetect", "exclude")
)
}
\arguments{
\item{scenario}{An \code{mp_scenario}.}

\item{nsim}{Simulations for the power estimate (default 1000).}

\item{calibrate_nsim}{Simulations for the null calibration (default \code{nsim}).}

\item{alpha}{Significance level (default 0.05).}

\item{seed}{Optional seed for reproducibility.}

\item{conf_level}{Confidence level for intervals (default 0.95).}

\item{ci_method}{Power CI type (see \code{\link[=mp_power]{mp_power()}}).}

\item{failure_policy}{Failure policy (see \code{\link[=mp_power]{mp_power()}}).}
}
\value{
An object of class \code{mp_plan}: a list with \code{calibration}, \code{power},
\code{recommendation}, \code{scenario}, and \code{alpha}.
}
\description{
The recommended end-to-end workflow: \strong{calibrate the null, choose an
inference method, then estimate power, and report both}. \code{mp_plan()} runs
\code{\link[=mp_calibrate]{mp_calibrate()}} and \code{\link[=mp_power]{mp_power()}} on the same scenario and bundles them with
an \code{\link[=mp_recommend_method]{mp_recommend_method()}} suggestion, so power is never reported without the
Type I error check that tells you whether it is trustworthy.
}
\examples{
\donttest{
if (requireNamespace("lme4", quietly = TRUE)) {
  d <- mp_design(list(subject = 12), trials_per_cell = 8)
  a <- mp_assumptions(
    fixed_effects = list("(Intercept)" = 0, condition = 0.4),
    random_effects = list(subject = list(intercept_sd = 0.5)),
    residual_sd = 1
  )
  scn <- mp_scenario_lme4(y ~ condition + (1 | subject), design = d, assumptions = a)
  mp_plan(scn, nsim = 100, seed = 1)
}
}
}
\seealso{
\code{\link[=mp_calibrate]{mp_calibrate()}}, \code{\link[=mp_power]{mp_power()}}, \code{\link[=mp_recommend_method]{mp_recommend_method()}}.
}
