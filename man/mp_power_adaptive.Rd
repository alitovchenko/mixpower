% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adaptive.R
\name{mp_power_adaptive}
\alias{mp_power_adaptive}
\title{Adaptive (sequential) power simulation}
\usage{
mp_power_adaptive(
  scenario,
  stop = mp_stop(),
  alpha = 0.05,
  seed = NULL,
  conf_level = 0.95,
  ci_method = c("clopper-pearson", "wald"),
  failure_policy = c("count_as_nondetect", "exclude"),
  check_calibration = TRUE,
  progress = FALSE
)
}
\arguments{
\item{scenario}{An \code{mp_scenario}.}

\item{stop}{An \code{\link[=mp_stop]{mp_stop()}} specification.}

\item{alpha, seed, conf_level, ci_method, failure_policy}{As in \code{\link[=mp_power]{mp_power()}}.}

\item{check_calibration}{As in \code{\link[=mp_power]{mp_power()}} (calibrate-first nudge); checked
once at the start.}

\item{progress}{Emit a per-batch progress message (default \code{FALSE}).}
}
\value{
An object of class \code{c("mp_power_adaptive", "mp_power")} — a standard
\code{mp_power} result plus a \verb{$stopping} list (\code{converged}, \code{reason},
\code{nsim_used}, \code{ci_halfwidth}, \code{target}, \code{rule}).
}
\description{
Runs \code{\link[=mp_power]{mp_power()}} in batches and stops as soon as the result is precise enough
or the accept/reject decision is clear (see \code{\link[=mp_stop]{mp_stop()}}), instead of relying
on an arbitrary fixed \code{nsim}. The achieved Monte Carlo precision (CI
half-width) and the number of replicates actually used are reported, so you
simulate no more than necessary.
}
\details{
Because replicate seeds are deterministic (\code{seed + i - 1}), the result for a
given stopping point is reproducible and identical to a fixed \code{mp_power()}
run at the same \code{nsim}.
}
\examples{
\donttest{
if (requireNamespace("lme4", quietly = TRUE)) {
  d <- mp_design(list(subject = 30), trials_per_cell = 6)
  a <- mp_assumptions(list("(Intercept)" = 0, condition = 0.5),
                      random_effects = list(subject = list(intercept_sd = 0.5)),
                      residual_sd = 1)
  scn <- mp_scenario_lme4(y ~ condition + (1 | subject), design = d, assumptions = a)
  mp_power_adaptive(scn, stop = mp_stop(ci_halfwidth = 0.05, batch = 50,
                                        min_nsim = 50, max_nsim = 400), seed = 1)
}
}
}
\seealso{
\code{\link[=mp_stop]{mp_stop()}}, \code{\link[=mp_power]{mp_power()}}, \code{\link[=mp_power_checkpoint]{mp_power_checkpoint()}}.
}
