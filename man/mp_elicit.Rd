% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elicit.R
\name{mp_elicit}
\alias{mp_elicit}
\title{Elicit assumptions from standardized scientific quantities}
\usage{
mp_elicit(
  family = c("gaussian", "binomial"),
  predictor = "condition",
  d = NULL,
  odds_ratio = NULL,
  risk_ratio = NULL,
  baseline_prob = NULL,
  outcome_sd = 1,
  icc = NULL,
  slope = NULL,
  cor = NULL,
  groups = "subject",
  intercept = NULL
)
}
\arguments{
\item{family}{\code{"gaussian"} or \code{"binomial"}.}

\item{predictor}{Name of the focal predictor (default \code{"condition"}).}

\item{d}{Standardized effect (Cohen's d) for \code{"gaussian"}.}

\item{odds_ratio, risk_ratio}{Effect for \code{"binomial"} (give one).}

\item{baseline_prob}{Control-group event probability for \code{"binomial"}.}

\item{outcome_sd}{Total outcome SD for \code{"gaussian"} (default 1).}

\item{icc}{Intraclass correlation: a single value (applied to all \code{groups})
or a vector named by group. For \code{"gaussian"} the values must sum to \verb{< 1}.}

\item{slope}{Random-slope SD as a fraction of the random-intercept SD: a
single value or named by group (\code{NULL} for none).}

\item{cor}{Intercept-slope correlation: a single value or named by group.}

\item{groups}{Grouping-factor name(s) (default \code{"subject"}).}

\item{intercept}{Fixed intercept; defaults to 0 (gaussian) or
\code{qlogis(baseline_prob)} (binomial).}
}
\value{
An object of class \code{c("mp_elicitation", "mp_assumptions")} usable
anywhere \code{\link[=mp_assumptions]{mp_assumptions()}} is; its \code{print()} shows the elicited inputs and
the implied model.
}
\description{
Build a coherent \code{\link[=mp_assumptions]{mp_assumptions()}} object from the quantities researchers
actually reason about --- intraclass correlations, a standardized effect, a
baseline event probability and an odds/risk ratio, and random-slope
variability --- instead of hand-picking raw regression coefficients and
standard deviations. \code{mp_elicit()} solves for an internally consistent
parameter set and prints the \strong{implied model}, combining mlmpower-style
elicitation with mixpower's simulation and calibration tooling.
}
\details{
For \code{family = "gaussian"} the total outcome variance is partitioned by the
per-group \code{icc} (each random-intercept variance is \code{icc * outcome_sd^2}, the
residual variance is the remainder), and the fixed effect is \code{d * outcome_sd}.
For \code{family = "binomial"} the intercept is \code{qlogis(baseline_prob)}, the effect
is \code{log(odds_ratio)} (a \code{risk_ratio} is converted given \code{baseline_prob}), and
each random-intercept SD is derived from the latent-scale \code{icc}
(\verb{residual variance = pi^2 / 3}). A random slope, when requested, is set to
\code{slope} times the random-intercept SD.

Predictor correlations, autocorrelation, and cluster-size distributions are
design choices (see \code{\link[=mp_design]{mp_design()}}); missingness is \code{\link[=mp_missing]{mp_missing()}}.
}
\examples{
# Gaussian: medium effect, ICC = 0.1
mp_elicit("gaussian", d = 0.5, icc = 0.1)

# Binomial: 20\% baseline event rate, odds ratio 1.8, subject ICC 0.05
mp_elicit("binomial", baseline_prob = 0.2, odds_ratio = 1.8, icc = 0.05)
}
\seealso{
\code{\link[=mp_assumptions]{mp_assumptions()}}, \code{\link[=mp_d_to_beta]{mp_d_to_beta()}}, \code{\link[=mp_icc_to_sd]{mp_icc_to_sd()}}.
}
