% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adaptive.R
\name{mp_stop}
\alias{mp_stop}
\title{Stopping rule for adaptive power simulation}
\usage{
mp_stop(
  ci_halfwidth = 0.02,
  target = NULL,
  min_nsim = 200,
  max_nsim = 5000,
  batch = 200
)
}
\arguments{
\item{ci_halfwidth}{Target half-width of the power CI (e.g. \code{0.02}), or \code{NULL}
to use only the decision rule.}

\item{target}{Optional target power (in (0, 1)); enables the decision rule.}

\item{min_nsim}{Minimum replicates before stopping is considered (default 200).}

\item{max_nsim}{Maximum replicates (hard cap, default 5000).}

\item{batch}{Replicates added per iteration (default 200).}
}
\value{
An object of class \code{mp_stop}.
}
\description{
Specifies when \code{\link[=mp_power_adaptive]{mp_power_adaptive()}} should stop adding simulation batches.
Stopping is triggered (after at least \code{min_nsim} replicates) when \emph{either}:
}
\details{
\itemize{
\item \strong{precision}: the power confidence interval's half-width is \code{<=}
\code{ci_halfwidth}, or
\item \strong{decision}: a \code{target} power is given and the interval lies entirely
above or entirely below it (the accept/reject decision is unambiguous).
}

Otherwise it keeps adding batches until \code{max_nsim} is reached.
}
\examples{
mp_stop(ci_halfwidth = 0.02, max_nsim = 4000)
mp_stop(target = 0.8, ci_halfwidth = 0.03)
}
\seealso{
\code{\link[=mp_power_adaptive]{mp_power_adaptive()}}.
}
